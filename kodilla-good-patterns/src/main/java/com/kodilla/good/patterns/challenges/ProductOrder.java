package com.kodilla.good.patterns.challenges;

public class ProductOrder implements ProductService {
    @Override
    public boolean isOrdered(User user, Product product) {
        return true;
    }
}
